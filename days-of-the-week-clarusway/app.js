const day = prompt("enter a day:").toLowerCase();
// const days = ["monday", "thursday", "wednesday", "saturday", "sunday"];
/* if (day == "monday" || "thursday" || "wednesday") {
  console.log("single session");
} else {
  if (day == "saturday") {
    console.log("double session");
  } else {
    console.log("There is no session");
  }
} */
// condition ? (condition2?expression3:expression4):expression2
/* day == ("monday" || "thursday" || "wednesday")
  ? console.log("single session")
  : day == "saturday"
  ? console.log("double session")
  : console.log("There is no session"); */
  // ayni isi mapping function ile yapma